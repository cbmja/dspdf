<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.daishin.pdf.mapper.MasterMapper">

    <insert id = "save" parameterType="Master">
        INSERT INTO dbo.MASTER
        (TR_KEY
        , TOTAL_SEND_CNT
        , DLV_TYPE_CD
        , PRINT_TYPE_NM
        , PAGE_CNT
        , API_KEY
        , RET_YN
        , PDF_YN)
        VALUES
        (#{trKey}
        , #{totalSendCnt}
        , #{dlvTypeCd}
        , #{printTypeNm}
        , #{pageCnt}
        , #{apiKey}
        , #{retYn}
        , #{pdfYn})
    </insert>

    <select id = "findByTrKey" parameterType="String" resultType="Master">
        SELECT * FROM dbo.MASTER WHERE TR_KEY = #{trKey}
    </select>


</mapper>